# Find_me
Find_Me is a lightweight Python tool designed to discover hidden or common endpoints on a web server using a wordlist. It sends HTTP requests to possible paths and reports accessible endpoints based on HTTP status codes.


If you are using linux 🐧:
git clone https://github.com/yourname/find_me
cd find_me
chmod +x find_me
sudo cp find_me /usr/local/bin/
